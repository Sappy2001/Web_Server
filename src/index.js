import React from 'react';
import ReactDOM  from 'react-dom';

import App from './App';
//our entire react application is inside App component
//hook App component in root div which is in public fold index.html
ReactDOM.render(<App/>,document.getElementById('root'));